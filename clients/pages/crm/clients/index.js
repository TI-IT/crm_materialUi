import React, { useState } from 'react';
import AddClients from '../../../crm/clients/addClients';

const Clients = () => {
    return (
        <div>
            <AddClients />
        </div>
    );
};

export default Clients;
